#include"List.hpp"
#include<list>
using namespace std;
#if 1
int main()
{
	AHAOAHA::List<int> l;
	l.pushback(1);
	l.pushback(2);
	l.pushback(3);
	l.print();
 	return 0;
}
#endif

#if 0
int main()
{
	list<int> l;
	list<int>::iterator it;
	return 0;
}
#endif